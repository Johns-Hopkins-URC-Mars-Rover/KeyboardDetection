# KeyboardDetection
Detecting aruco tags on corners of keyboard then detecting the keys on the Red-dragon Keyboard

## Setup

```bash
conda create -n aruco-keyboard python=3.10
conda activate aruco-keyboard
pip install -r requirements.txt
```